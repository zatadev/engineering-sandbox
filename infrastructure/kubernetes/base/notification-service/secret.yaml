apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  namespace: sandbox
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: backend
type: Opaque
# Values are base64-encoded. In production, use Vault or Sealed Secrets.
# echo -n 'value' | base64
data:
  rabbitmq-password: Z3Vlc3Q=  # guest
